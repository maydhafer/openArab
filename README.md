<div dir="rtl" align="right">

<img src="https://raw.githubusercontent.com/openclaw/openclaw/main/assets/openclaw-logo.png" alt="OpenClaw" width="140"/>

# 🦞 OpenArab
### النسخة العربية من OpenClaw

> وكيل ذكاء اصطناعي شخصي يشتغل على جهازك — ويردّ عليك من واتساب، تيليغرام، ديسكورد، وأكثر.

[![Discord](https://img.shields.io/badge/Discord-انضم_للمجتمع-5865F2?logo=discord&logoColor=white)](https://discord.gg/openclaw)
[![GitHub](https://img.shields.io/github/stars/openclaw/openclaw?style=social)](https://github.com/openclaw/openclaw)
[![Docs](https://img.shields.io/badge/التوثيق_الرسمي-docs.openclaw.ai-0ea5e9)](https://docs.openclaw.ai)

---

## 📖 الفهرس

- [ما هو OpenClaw؟](#-ما-هو-openclaw)
- [قبل ما تبدأ — المتطلبات](#-قبل-ما-تبدأ--المتطلبات)
- [الخطوة ١ — تثبيت البرنامج](#-الخطوة-١--تثبيت-البرنامج)
- [الخطوة ٢ — معالج الإعداد](#-الخطوة-٢--معالج-الإعداد)
- [الخطوة ٣ — تشغيل البوابة](#-الخطوة-٣--تشغيل-البوابة)
- [الخطوة ٤ — ربط تطبيق المحادثة](#-الخطوة-٤--ربط-تطبيق-المحادثة)
- [الخطوة ٥ — اختبار أول رسالة](#-الخطوة-٥--اختبار-أول-رسالة)
- [حل المشاكل الشائعة](#-حل-المشاكل-الشائعة)
- [روابط مفيدة](#-روابط-مفيدة)

---

## 🤖 ما هو OpenClaw؟

**OpenClaw** بوابة ذكاء اصطناعي مفتوحة المصدر تشغّلها على **جهازك الشخصي**.

بدل ما تفتح موقع أو تطبيق منفصل — ترسل رسالة عادية من أي تطبيق تستخدمه، والذكاء الاصطناعي يردّ عليك مباشرة.

```

أنت (واتساب / تيليغرام / ديسكورد)

           ↓

      OpenClaw Gateway

      (يشتغل على جهازك)

           ↓

    وكيل الذكاء الاصطناعي

           ↓

      الرد يوصلك فوراً
```

**يشتغل على:** macOS ✅ &nbsp; Linux ✅ &nbsp; Windows (WSL2) ✅

---

## 📋 قبل ما تبدأ — المتطلبات

تحتاج شيئين فقط قبل التثبيت:

### ١. Node.js الإصدار 22 أو أحدث

افتح الـ Terminal وتحقق من إصدارك:

```bash

node --version
```

**النتيجة المطلوبة:** رقم يبدأ بـ `v22` أو أعلى — مثل `v22.13.0`

> ⚠️ إذا ظهر رقم أقل من 22، أو ظهرت رسالة خطأ — [حمّل Node.js من هنا](https://nodejs.org) واختر **LTS**.
> 💡 إذا استخدمت سكريبت التثبيت (الطريقة الأولى أدناه)، يثبّت Node تلقائياً بدون ما تحتاج تسوي شيء.

---

### ٢. مفتاح API

تحتاج مفتاح من أحد مزودي الذكاء الاصطناعي. **Anthropic (Claude) هو الموصى به.**

- [احصل على مفتاح Anthropic](https://console.anthropic.com)

احتفظ بالمفتاح — معالج الإعداد سيطلبه منك لاحقاً.

---

## 🚀 الخطوة ١ — تثبيت البرنامج

عندك **طريقتان** للتثبيت. اختر الأنسب لك:

---

### 🟢 الطريقة الأولى: سكريبت التثبيت التلقائي *(الأسهل — موصى بها)*

أمر واحد يثبّت كل شيء ويشغّل معالج الإعداد تلقائياً.

**macOS أو Linux أو WSL2 على Windows:**

```bash

curl -fsSL https://openclaw.ai/install.sh | bash
```

**Windows (PowerShell فقط):**

```powershell

iwr -useb https://openclaw.ai/install.ps1 | iex
```

> ✅ السكريبت يتحقق من Node، يثبّته إذا ما كان موجوداً، ثم يشغّل معالج الإعداد مباشرة.
> بعد ما ينتهي السكريبت، انتقل مباشرة إلى الخطوة ٢.

---

### 🔵 الطريقة الثانية: npm اليدوي *(إذا عندك Node 22+ مثبّت مسبقاً)*

**الخطوة أ — تثبيت OpenClaw:**

```bash

npm install -g openclaw@latest
```

انتظر حتى ينتهي التثبيت. قد يأخذ دقيقة أو دقيقتين.

**تحقق من نجاح التثبيت:**

```bash

openclaw --version
```

يجب أن يظهر رقم الإصدار. إذا ظهر → انتقل للخطوة ٢.

> ⚠️ **ظهرت رسالة "openclaw: command not found"؟** راجع قسم حل المشاكل الشائعة أدناه.

---

## 🧙 الخطوة ٢ — معالج الإعداد

هذا الأمر يضبط كل إعدادات البرنامج خطوة بخطوة:

```bash

openclaw onboard --install-daemon
```

**ماذا سيحدث:**

| المرحلة | ما يطلبه المعالج |
|---------|-----------------|
| ١ | مفتاح الـ API (الذي حصلت عليه في المتطلبات) |
| ٢ | إعدادات البوابة (المنفذ وغيره) |
| ٣ | ربط قنوات المحادثة (اختياري في هذه المرحلة) |
| ٤ | تثبيت الخدمة كـ daemon تشتغل تلقائياً عند تشغيل الجهاز |

> 💡 **`--install-daemon`** يعني إن OpenClaw يشتغل تلقائياً في الخلفية كلما شغّلت جهازك — بدون ما تحتاج تشغّله يدوياً كل مرة.

---

## ▶️ الخطوة ٣ — تشغيل البوابة

### تحقق من أن البوابة تعمل:

```bash

openclaw gateway status
```

**النتيجة المتوقعة:** رسالة تقول إن البوابة تعمل (running).

---

### افتح لوحة التحكم في المتصفح:

```bash

openclaw dashboard
```

أو افتح هذا الرابط مباشرة في متصفحك:

```

http://127.0.0.1:18789/
```

> ✅ إذا فتحت لوحة التحكم بنجاح — البوابة تعمل وأنت جاهز للخطوة التالية.

---

### تشغيل البوابة يدوياً (للاختبار أو التشخيص):

```bash

openclaw gateway --port 18789
```

> اتركه يشتغل في نافذة الـ Terminal أثناء الاختبار.

---

## 🔌 الخطوة ٤ — ربط تطبيق المحادثة

الآن تربط التطبيق اللي تبي تستخدمه:

```bash

openclaw channels login
```

سيظهر لك قائمة بالتطبيقات المتاحة (واتساب، تيليغرام، ديسكورد...) — اختر واتبع التعليمات.

> 📖 تعليمات تفصيلية لكل تطبيق: [docs.openclaw.ai/channels](https://docs.openclaw.ai/channels)

---

## 💬 الخطوة ٥ — اختبار أول رسالة

بعد ربط القناة، اختبر إن كل شيء يشتغل:

```bash

openclaw message send --target +966XXXXXXXXX --message "مرحبا، هل تسمعني؟"
```

> استبدل `+966XXXXXXXXX` برقمك أو رقم الاختبار.

**إذا وصل الرد** → مبروك، كل شيء يشتغل! 🎉
**إذا ما وصل** → راجع قسم حل المشاكل أدناه.

---

## 🩺 حل المشاكل الشائعة

### المشكلة ١: `openclaw: command not found` بعد التثبيت

**السبب:** مسار npm العالمي مو موجود في الـ PATH.

**الحل:**

```bash
الخطوة أ: اعرف أين ثبّت npm البرنامج
npm prefix -g

الخطوة ب: تحقق من الـ PATH الحالي
echo $PATH


إذا المسار الظاهر في الخطوة أ مو موجود في الخطوة ب، أضفه:

```bash
أضف هذا السطر في نهاية ملف ~/.zshrc أو ~/.bashrc
export PATH="$(npm prefix -g)/bin:$PATH"


ثم أعد تحميل الـ Terminal:

```bash

source ~/.zshrc
أو
source ~/.bashrc


---

### المشكلة ٢: خطأ في التثبيت يخص `sharp`

**الحل:**

```bash

SHARP_IGNORE_GLOBAL_LIBVIPS=1 npm install -g openclaw@latest
المشكلة ٣: البوابة ما تشتغل
شغّل أداة التشخيص:


openclaw doctor   # يكشف مشاكل الإعداد

openclaw status   # يعرض الحالة الكاملة
المشكلة ٤: Windows — الأوامر ما تشتغل
تأكد إنك تستخدم WSL2 وليس PowerShell العادي لأوامر Linux.
دليل تثبيت WSL2

📚 روابط مفيدة
الرابط	الوصف
docs.openclaw.ai	📖 التوثيق الرسمي الكامل
docs.openclaw.ai/install	🔧 طرق التثبيت المختلفة (Docker، Nix، Ansible...)
docs.openclaw.ai/channels	🔌 إعداد قنوات المحادثة
docs.openclaw.ai/start/wizard	🧙 تفاصيل معالج الإعداد
github.com/openclaw/openclaw	💻 الكود المصدري
Discord	💬 مجتمع الدعم
🤝 المساهمة في OpenArab
هذا المشروع ترجمة عربية مجتمعية لـ OpenClaw.
نرحب بأي مساهمة — ترجمة، توثيق، تحسينات، أو تصحيح أخطاء.

<sub>بكل محبة من 🇸🇦 — <a href="https://github.com/alraigah88">مي للتقنية</a></sub>

</div> ````
